<UserControl x:Class="UmojoParkingPoC.DockPanes.AssetManagerDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:local="clr-namespace:UmojoParkingPoC.DockPanes"
             xmlns:fw="clr-namespace:UmojoParkingPoC.Framework"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance Type=local:AssetManagerDockPaneViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <fw:StatusToBrushConverter x:Key="StatusToBrush"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Auth header -->
        <Border Grid.Row="0" Padding="10,8"
                Background="{DynamicResource Esri_BackgroundPanelBrush}"
                BorderBrush="{DynamicResource Esri_BorderBrush}"
                BorderThickness="0,0,0,1">
            <TextBlock Text="{Binding AuthStatusText}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Esri_TextStyleEmphasizedBrush}"/>
        </Border>

        <!-- Body -->
        <Grid Grid.Row="1">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                       Text="Sign in to view assets"
                       Foreground="{DynamicResource Esri_TextStyleDisabledBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsAuthenticated}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Grid Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,8">
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" MinWidth="80"/>
                </StackPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Assets}"
                          SelectedItem="{Binding SelectedAsset, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Margin="10,0,10,4">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding AssetType}" Width="Auto"/>
                        <DataGridTextColumn Header="Rate" Binding="{Binding HourlyRate, StringFormat={}{0:C}}" Width="Auto"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>

        <!-- Edit form -->
        <Border Grid.Row="2" Padding="10,10"
                Background="{DynamicResource Esri_BackgroundPanelBrush}"
                BorderBrush="{DynamicResource Esri_BorderBrush}"
                BorderThickness="0,1,0,0">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedAsset}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsAuthenticated}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="Edit selected asset"
                           FontWeight="SemiBold" Margin="0,0,0,6"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,8,4"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Hourly rate" VerticalAlignment="Center" Margin="0,0,8,4"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EditHourlyRate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Time limit (min)" VerticalAlignment="Center" Margin="0,0,8,4"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding EditTimeLimit, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Status" VerticalAlignment="Center" Margin="0,0,8,4"/>
                <ComboBox Grid.Row="4" Grid.Column="1"
                          ItemsSource="{Binding StatusOptions}"
                          SelectedItem="{Binding EditStatus, Mode=TwoWay}"
                          Margin="0,0,0,4"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Last modified" VerticalAlignment="Center" Margin="0,4,8,0"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding LastModifiedDisplay}"
                           Foreground="{DynamicResource Esri_TextStyleDisabledBrush}"
                           VerticalAlignment="Center" Margin="0,4,0,0"/>
            </Grid>
        </Border>

        <!-- Save button + status bar -->
        <Border Grid.Row="3" Padding="10,6"
                Background="{DynamicResource Esri_BackgroundPanelBrush}"
                BorderBrush="{DynamicResource Esri_BorderBrush}"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="Save Changes"
                        Command="{Binding SaveCommand}" HorizontalAlignment="Right"
                        MinWidth="120"
                        Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVis}}"/>
                <ProgressBar Grid.Row="1" Height="3" IsIndeterminate="True"
                             Margin="0,6,0,0"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Grid.Row="2" Text="{Binding StatusMessage}"
                           Margin="0,4,0,0"
                           FontWeight="SemiBold"
                           Foreground="{Binding StatusMessage, Converter={StaticResource StatusToBrush}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
